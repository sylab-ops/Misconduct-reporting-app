<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sylabblueprint.chatbee">
    <!-- Internet permission -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- Camera permission -->
    <uses-permission android:name="android.permission.CAMERA" /> <!-- Internet permission -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ConfirmAppointmentActivity"></activity>
        <activity android:name=".createDiaryActivity" />
        <activity android:name=".PreviewActivity" />
        <activity android:name=".DesiredoutcomeActivity" />
        <activity android:name=".WhereActivity" />
        <activity android:name=".PhysicalActivity" />
        <activity android:name="com.sylabblueprint.DesiredOutcomeActivity" />
        <activity android:name=".whoActivity" />
        <activity android:name=".WhenActivity" />
        <activity android:name=".WhatActivity" />
        <activity android:name=".AccountTypeActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ContactsActivity" />
        <activity android:name=".DiaryHistoryActivity" />
        <activity android:name=".BehaviourCategoryActivity" />
        <activity android:name=".ReportTypesActivity" />

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@drawable/rounded_button" />

        <activity android:name=".main_dashboardActivity" />
        <activity android:name=".InfoActivity" />
        <activity android:name=".DiaryActivity" />
        <activity
            android:name=".adapters.ChatActivity"
            android:theme="@style/AppThemeNo" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>